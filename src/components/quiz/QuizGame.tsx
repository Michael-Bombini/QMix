import React from 'react'
import { UserChoices } from '../../interfaces/UserChoices'

export default function QuizGame({username, difficulty, categoryId , categoryName}: UserChoices) {
  return (
    <div></div>
  )
}
